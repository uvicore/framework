# 2026-06-17

> Backfilled from git history (`c7dce34`, `0717309`, `f070a30`).

## Introduced Claude (.claude skills) + major test expansion — `c7dce34`
- **Changed:** Added the `.claude/skills/` set (architecture, orm, http, database, console-events-jobs,
  testing, add-subsystem) and the root `CLAUDE.md`. Added substantial new test coverage: API
  integration (`test_api/` — auto-API CRUD/query/write/with-relations, guard permissions), auth
  `user_info` properties, and ORM multiple-many includes + polymorphic mutations. Fixed key ORM bugs.
- **Files:** `.claude/skills/*`, `CLAUDE.md`, `tests/test_api/*`, `tests/test_auth/*`,
  `tests/test_db/test_orm/*`, `uvicore/orm/model.py`, `uvicore/orm/query.py`,
  `uvicore/http/routing/model_router.py`.

## More DB WHERE operators, more dialects, Postgres fixes, cross-DB docker tests — `0717309`
- **Intent:** Broaden database support and query expressiveness; verify against real engines.
- **Changed:** Expanded `db.py` `SUPPORTED_DIALECTS` (added mariadb, mssql, oracle, cockroachdb,
  snowflake; `postgres` alias) plus per-dialect driver/port defaults and async-driver detection.
  Added WHERE operators to the query builder (`like`, `ilike`, `in`, `not in`, `between`, `null`,
  `not null`, `!= < <= > >=`). Added docker-based cross-DB integration tests (postgres/mysql/mariadb).
- **Files:** `uvicore/database/db.py`, `uvicore/database/builder.py`, `uvicore/orm/query.py`,
  `uvicore/orm/mapper.py`, `tests/integration/*`, `tests/test_db/test_orm/*`.
- **Related:** [ADR 0001](../adr/0001-expanded-sql-dialects-and-where-operators.md).

## Removed legacy `.github` Copilot folder — `f070a30`
- **Changed:** Dropped the old GitHub Copilot instructions in favor of the stronger `.claude` skills.
