# 2026-06-20

> Backfilled from git history (`4a5ae60`, `31e82c7`).

## Test-app + httpx teardown fixes
- **Changed:** Fixed the `app1` test app to use the new `environs` `env.read_env` method (`4a5ae60`);
  added a defensive condition to the httpx teardown to avoid errors on shutdown (`31e82c7`).
- **Files:** `tests/conftest.py`, test app config, httpx client teardown.
- **Related:** follows the httpx standardization ([ADR 0004](../adr/0004-httpx-over-aiohttp.md)).
