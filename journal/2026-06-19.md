# 2026-06-19

> Backfilled from git history. A very large day — the Pydantic v2 / dependency-modernization push.
> Commits: `94923d6`, `08a22e4`, `22cfe17`, `2b8b893`, `0f51b61`, `d3e3c43`, `d0c4821`, `fcb61eb`.

## Pydantic v1 → v2 + modern FastAPI / Starlette — `0f51b61`
- **Intent:** Unblock the modern web stack — FastAPI dropped Pydantic v1 (0.126.0) and removed the v1
  shim (0.128.0), so Pydantic 1.10 was a hard ceiling.
- **Changed:** `pydantic 1.10 → 2.x`, `starlette 0.45.3 → 1.3.1`, `fastapi 0.115.7 → 0.137.2`.
  Extensive ORM work (`metaclass.py`, `model.py`) for v2; updates to model_router, response,
  controllers, middleware; removed per-model `update_forward_refs()` in favor of a central rebuild.
  All tests passing.
- **Files:** `pyproject.toml`, `uvicore/orm/metaclass.py`, `uvicore/orm/model.py`,
  `uvicore/http/*`, `uvicore/auth/user_info.py`, `uvicore/database/package/bootstrap.py`.
- **Related:** [ADR 0002](../adr/0002-modernize-web-stack-pydantic-v2.md),
  [ADR 0003](../adr/0003-central-model-rebuild.md).

## SQLAlchemy / async-driver stack upgrade — `08a22e4`
- **Changed:** `sqlalchemy` held at `2.0.*`; `alembic 1.14 → 1.18`, `aiomysql 0.2 → 0.3`,
  `aiosqlite 0.20 → 0.22`, `asyncpg 0.30 → 0.31`. All tests passing.
- **Files:** `pyproject.toml`, `poetry.lock`.

## Redis 5 → 8 + full Redis/cache integration testing — `22cfe17`, `2b8b893`
- **Changed:** Added redis + cache docker integration suites and fixed cache backend bugs
  (array/redis). Upgraded `redis[hiredis] 5.2 → 8.0.0`. Renamed `test-integration.sh` →
  `test-db-integration.sh`; added `test-redis-integration.sh` / `test-cache-integration.sh`.
- **Files:** `uvicore/cache/backends/*`, `tests/integration/*`, `bin/test-*-integration.sh`,
  `pyproject.toml`.

## Drop aiohttp, standardize on httpx — `d3e3c43`
- **Changed:** Removed `aiohttp (3.11)` in favor of `httpx (0.28.1)`; updated the `http_client`
  service and the mailgun mail backend; tests use `httpx.AsyncClient` directly.
- **Files:** `uvicore/http_client/package/provider.py`, `uvicore/mail/backends/mailgun.py`,
  `tests/conftest.py`, `pyproject.toml` (poetry.lock −707 lines).
- **Related:** [ADR 0004](../adr/0004-httpx-over-aiohttp.md).

## Python 3.14 support; minimum raised to 3.12 — `fcb61eb`
- **Changed:** `requires-python >=3.10 → >=3.12`; verified passing on 3.12 / 3.13 / 3.14 (dev on
  Python 3.14.6). Minor fixes in `events/dispatcher.py`, `support/concurrency.py`, redis provider.
- **Files:** `.python-version`, `pyproject.toml`, `uvicore/__init__.py`.
- **Related:** [ADR 0005](../adr/0005-pipe-style-typing-and-python-floor.md).

## Remaining dependency upgrades + sub-dependency pinning tool — `d0c4821`
- **Changed:** Updated all remaining deps to latest; added `list-subdependencies.sh` to help pin
  transitive deps properly.
- **Files:** `pyproject.toml`, `poetry.lock`, `list-subdependencies.sh`.

## Fix ORM inline tables + full event test suite — `94923d6`
- **Changed:** Fixed ORM inline-table handling (`metaclass.py`); added `test_inline_table.py` and a
  comprehensive `tests/test_events/` suite (replacing the old monolithic `test_events.py`).
- **Files:** `uvicore/orm/metaclass.py`, `tests/test_events/*`, `tests/test_db/test_orm/test_inline_table.py`.
