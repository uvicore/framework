# 2026-06-22

> Backfilled from git history (`35a6ee7`, `1847f9b`, `37621ce`, `8b1af4a`).

## Pipe-style typing convention + uvicore.typing/stdlib boundary cleanup — `35a6ee7`
- **Intent:** Standardize on modern typing and stop crossing the `uvicore.typing` vs stdlib `typing`
  boundary inconsistently.
- **Changed:** Repo-wide switch from `Optional[X]` / `Union[...]` to pipe-style `X | None` / `A | B`;
  clarified the `uvicore.typing` (Dict/SuperDict/...) vs stdlib boundary. ~126 files touched,
  including `orm/fields.py`, `orm/query.py`, `orm/metaclass.py`, the HTTP routers, and
  `typing/__init__.py`. Follow-ups: pipe-style in test models (`1847f9b`), removed a stale client
  method reference in tests (`37621ce`), `.gitignore` update (`8b1af4a`).
- **Files:** broad — `uvicore/**`, `tests/**`, `uvicore/typing/__init__.py`.
- **Related:** [ADR 0005](../adr/0005-pipe-style-typing-and-python-floor.md).
