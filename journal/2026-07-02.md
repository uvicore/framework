# 2026-07-02

## Fix view-composer dict-form registration + clean up composer stubs/generator (0.4.7)

- **Intent:** While documenting view composers in the docs repo, discovered the composer
  scaffolding still referenced the removed pre-0.4 `self.composers()` / `self.views()` provider
  helpers, and that the **dict form** of `register_http_view_composers()` was actually broken.
- **Bug fixed:** `uvicore/http/package/registers.py` — the `dict` branch of
  `register_http_view_composers()` called a non-existent `self.composers(mod, views)` and raised
  `AttributeError` on boot for anyone registering multiple composers via a dict.  Now recurses into
  `self.register_http_view_composers(mod, views)`.  (The single string/list form always worked.)
- **Scaffold/CLI cleanup (stale pre-0.4 API):**
  - `uvicore/http/commands/stubs/composer.py` — example comment now uses
    `register_http_view_composers(...)` instead of `self.composer(...)`.
  - `uvicore/http/commands/generators.py` — the `gen composer` "add this to your provider" hint now
    prints `self.register_http_view_composers('<module>', '<short_name>/*')` with the **correct**
    method name and argument order (was `self.composers('<short_name>/*', '<module>')`).
  - `tests/apps/app1/package/provider.py` — stale `self.composers()` comment reworded; the dict-form
    example de-duplicated (was the same key twice) and is now **active** so every app1 boot exercises
    the fixed dict-form path.
- **Tests:** new `tests/test_http/test_view_composers.py` (5 tests) — dict-form registration lands
  in `package.web.view_composers`, `@uvicore.composer()` IoC binding, aggregation into
  `uvicore.config.uvicore.http.view_composers`, end-to-end merge via `response.View()`, and that a
  controller-set context key wins over the composer (`.defaults()` merge).  Added
  `tests/test_http/conftest.py` `webserver` fixture (mirrors the api harness — rebuilds the HTTP
  server under pytest so templates + composer config are wired, no-op if already built) and app1
  composer/view fixtures (`http/composers/layout.py`, `http/composers/sidebar.py`,
  `http/views/app1/composed.j2`).  Full suite green: **540 passed**.
- **Version:** bumped `pyproject.toml` + `uvicore/__init__.py` to **0.4.7** (0.4.6 already shipped).
- **Docs (sibling `docs` repo):** new `http/web/view-composers.md`, wired into nav + web index,
  fixed the dead composer link in `web/templating.md`, and added the 0.4.7 changelog +
  release-notes entries.  Not committed (per user's commit policy).

## Rebuilt the app1 reference web app + two web-stack bug fixes (0.4.8)

- **Intent:** The `app1` test app's web (HTTP) layer had been started long ago against an external
  `mreschke.themes` package that no longer exists — `views/app1/{home,about}.j2` extended a
  non-existent `themes/{theme.name}/layout.j2` and depended on a `theme` context var, so the two
  main pages didn't render; the rest was littered with dead commented blocks and stub assets.
  Rebuilt the whole web portion, self-contained, to exercise **every** Uvicore Web feature so app1
  doubles as a living reference.
- **What changed (app1 fixture, `tests/apps/app1/`):**
  - **Removed the theme concept:** deleted `http/views/themes/**`, `http/views/errors/x404.j2`,
    `http/controllers/home2.py`, the `http/public/assets/auth/**` stub tree and the stray
    `http/assets/`; dropped the commented `mreschke.themes` block from `config/dependencies.py` and
    the orphaned dead commented tail of `config/package.py`.
  - **Layout + partials + macros:** new `views/layouts/app.j2` (blocks: title/head/content/sidebar/
    scripts), `views/partials/{header,footer,sidebar}.j2`, `views/macros/ui.j2`
    (`nav_link`/`field`/`alert`, imported `with context`).
  - **Composers:** rewrote `http/composers/layout.py` (feeds site-wide `nav`/`site_name`/`year` to
    the layout, **keeps** `composed_marker='app1-layout'`) and `sidebar.py` (adds `sidebar_links`,
    keeps `sidebar_marker`).  Registration in the provider is unchanged (test-pinned).
  - **Pages/controllers:** `home`, `about` (+`/about2` text), `features` (custom Jinja demos),
    `contact` (GET + form POST via `Form(...)`), `login`/`logout` (form POST + redirect), `admin`
    (guarded).  `http/routes/web.py` cleaned to controllers + a `scopes=['authenticated']` group.
    `http/views/app1/composed.j2` left verbatim (test dependency).
  - **Custom Jinja processors:** new `http/context.py` (`nav_active` context_function, `money`
    context_filter, `shout` filter, `prime` test); the provider's
    `register_http_view_context_processors({...})` block is now **active** (all four categories),
    and the `register_http_public('app1.http.publicc')` **typo fixed** to `app1.http.public`.
  - **Self-hosted assets:** `http/public/assets/css/app.css` (real modern stylesheet, light/dark),
    `js/app.js` (mobile-nav toggle), `img/logo.svg`; error pages `views/errors/404.j2` +
    `catch_all.j2` (standalone — they render outside the `app1/*` composer scope).
- **Framework bug fixes surfaced by the rebuild:**
  - `uvicore/http/exceptions/handlers.py` — the web handler rendered `errors/<code>.j2` /
    `catch_all.j2` but never passed `status_code`, so error pages returned **200**.  Now passes
    `status_code=status_code, headers=headers` on both template branches (the plain-HTML fallback
    already did).  A `404` template now truly responds `404`.
  - `uvicore/http/routing/guard.py` — a redirect-on-failed-auth configured as a **route name**
    (`app1.login`) ran `request.url_for(location)` → a Starlette `URL`, then `'?' in location`
    raised `TypeError: argument of type 'URL' is not a container or iterable`.  Now
    `str(request.url_for(location))`.  Full-URL redirects were unaffected; app1's
    `config/auth.py` `redirect='app1.login'` now works (verified: `/admin` → `301` to
    `http://testserver/login?referer=/admin`).
- **Tests:** new `tests/test_http/test_web_pages.py` (9 tests) drives the real ASGI stack via an
  httpx `AsyncClient` on the `webserver` fixture — home/about/features/contact(GET+POST)/login(GET+
  POST)/logout-redirect/about2/custom-404/guarded-admin.  Covers both framework fixes end to end.
  Full suite green: **549 passed**.  (`black-check.sh` not run — `black` isn't installed in this
  env; it only checks `uvicore/` and the edited lines match surrounding style.)
- **Version:** bumped `pyproject.toml` + `uvicore/__init__.py` to **0.4.8**.
- **Docs (sibling `docs` repo):** updated `http/exceptions/handlers.md` (embedded web-handler source
  + a note that error pages return the real status), added the 0.4.8 changelog + release-notes
  entries.  The redirect-by-route-name feature was already documented in `web/authentication.md`
  (it was broken, not changed), so no doc change there.  Not committed (per user's commit policy).

## Added app1 `serve-seed-sqlite` dev helper

- **Intent:** `./tests/apps/app1/uvicore http serve` fails at request time with
  `no such table: users` because the app1 (and auth) DB defaults to in-memory SQLite
  (`:memory:`) — per-process, and `http serve` never creates/seeds it.  Give a one-command way to
  browse the rebuilt web app.
- **Root cause detail:** the auth `users` table is on a **separate `auth` connection**
  (`uvicore/auth/database/tables/users.py` → `connection = 'auth'`, env prefix `DB_AUTH`).  In the
  pytest path app1+auth both point at `:memory:` and share one metakey/metadata, so the
  `comments.creator_id → users` FK resolves; redirecting only `DB_APP1_DB` splits them and the FK
  breaks.  Fix is to point **both** `DB_APP1_DB` and `DB_AUTH_DB` at the same SQLite *file*.
- **Added:** `tests/apps/app1/serve-seed-sqlite` (executable) — sets both env vars to one file
  (default `${TMPDIR:-/tmp}/app1.sqlite`, override `DB_FILE`), runs `db reseed app1` (drop/create/
  seed), then `exec ... http serve` (default port 5001, override `SERVER_PORT`/`SERVER_HOST`).
  Flags: `--no-seed` (serve existing file), `--help`.  Prefers `poetry run` when available, else
  calls the `./uvicore` launcher directly like the sibling `serve-uvicorn`/`serve-gunicorn` scripts.
  No `.env` (per request) — a committed `.env` would also flip the pytest suite off `:memory:`.
- **Verified:** reseed writes users=6/posts=7; server returns `/`→200 (home renders),
  `/admin`→301 `/login?referer=/admin`, unknown path→404 (custom page); `--no-seed` skips reseed and
  serves the existing file.
